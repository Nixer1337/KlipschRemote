[pytest]
testpaths = tests
# Never descend into build staging / outputs (they vendor the whole CPython
# stdlib, which would otherwise be collected and slow discovery to a crawl).
norecursedirs = .build_app dist dist_app .git .pytest_cache
